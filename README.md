This contains the sql database file, original data file, a python script to convert the file data into tables, and the output of the script

In order to create database:
1. Copy text from database_data.txt 